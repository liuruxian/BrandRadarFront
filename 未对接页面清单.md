# 未完成后端对接页面清单（持续更新）

> 更新时间：2026-04-02  
> 说明：本清单用于回查“哪些页面元素尚未接入后端接口”，并区分是“前端未调用已有接口”还是“后端缺少接口”。

---

## 已接入完成（本次确认）

- 仪表盘 `src/views/DashboardView.vue`
- 产品中心 `src/views/ProductListView.vue`
- 价格监控 `src/views/PriceMonitorView.vue`
- 调度管理 `src/views/SchedulerConfigView.vue`
- 系统监控 `src/views/admin/SystemDashboardView.vue`
- 配置中心 `src/views/admin/SystemConfigView.vue`
- 用户管理 `src/views/admin/UserManageView.vue` ✅（本次已改为真实接口）
- 会话管理 `src/views/admin/SessionManagerView.vue` ✅（本次已改为真实接口）

---

## 未完成对接页面（需后续处理）

## 1) 系统公告（按当前要求：暂不对接）
- 页面：`src/views/admin/AnnouncementView.vue`
- 当前状态：公告读取/发布均为本地状态演示。
- 页面内未对接元素：
  - 当前公告读取
  - 发布公告提交
- 对接方案（接口已存在）：
  - `GET /api/admin/announcement`（`adminApi.getAnnouncement`）
  - `PUT /api/admin/announcement`（`adminApi.updateAnnouncement`）
- 结论：**前端未接，但接口已具备**。

## 2) 个人中心
- 页面：`src/views/ProfileCenterView.vue`
- 当前状态：
  - 修改密码已接：`POST /api/auth/password`
  - 个人资料保存仍是前端 `alert` 演示
- 页面内未对接元素：
  - 昵称/邮箱/手机号保存
- 结论：**存在缺失接口（见下节）**。

---

## 缺失接口整理（后端待补）

## A) 个人资料更新接口（缺失）
当前前端需要但未在现有 API 封装中落地：

1. 获取当前用户资料（含昵称、邮箱、手机号）
- 建议：`GET /api/users/me`
- 返回建议字段：
  - `username`
  - `nickname`
  - `email`
  - `phone`
  - `role`

2. 更新当前用户资料
- 建议：`PUT /api/users/me`
- 请求体建议字段：
  - `nickname?: string`
  - `email?: string`
  - `phone?: string`

> 备注：若后端已有等价接口（例如 `/api/profile`），前端可直接改为对接现有路径。

---

## 下次对接建议顺序

1. 公告（你要求暂不接，可后置）
2. 个人中心资料（需要后端先补接口）

---

## 变更记录

- 2026-04-02：用户管理页已由 mock 改为真实 API（用户与角色增删改查）。
- 2026-04-02：会话管理页已由 mock 改为真实 API（会话列表 + 强制下线）。
